{
  "name": "server",
  "version": "1.0.0",
  "description": "What the heck am I doing. I don't know I'm just trying to figure out server side programming",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "dev": "concurrently -n 'server,client' \"nodemon server.js\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.14"
  },
  "dependencies": {
    "concurrently": "^6.3.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^4.1.3",
    "morgan": "^1.10.0"
  }
}
